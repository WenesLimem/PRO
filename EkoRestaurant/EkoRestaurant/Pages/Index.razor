@page "/"
@page "/index"
@using EkoRestaurant.Services
@inject DailyMenusService DailyMenusService
@inject NavigationManager _NavigationManager

<MudGrid Spacing="5" Justify="Justify.Center">
    <MudItem xs="12">
        <MudPaper Class="d-flex align-center justify-space-between pa-2 my-1 mx-2 mt-6" Outlined="false" Elevation="0">
            <MudText Typo="Typo.h2">
                <AuthorizeView>
                     Bienvenue @context.User.Identity.Name! !
                </AuthorizeView>

</MudText>
            <MudButton Style="float: right" Class="btn-mud-gradiant-bg-rounded h6-quicksand" Variant="Variant.Filled" OnClick="RedirectToCommandCreation">
                <img src="Images/white-add-plus.svg" />Nouvelle commande
            </MudButton>
        </MudPaper>
    </MudItem>

    <MealInfo />
    <MealInfo />
    <MealInfo />
    <!--
        <MealInfo CurrentRecipe="CurrentDailyMenu.Starter" />
        <MealInfo CurrentRecipe="CurrentDailyMenu.MainDish"/>
        <MealInfo CurrentRecipe="CurrentDailyMenu.Dessert"/>
    -->
</MudGrid>

@code {
    // Faire la récupération des données depuis la DB
    // public DailyMenu CurrentDailyMenu { get; set; }
    
    private void RedirectToCommandCreation()
    {
        _NavigationManager.NavigateTo("/commands-creation");
    }

}


